/*
Diagrama de Classes

-->Publico (+)

-->Privado (-)

-->Protegido (=)


--------------------------------TIPOS DE RELACIONAMENTOS----------------------------------------------

--generalização =~ herança
--Dependência :
    Tem duas classes que elas existem de forma independente (distintas) mas que uma depende da outra para que ela funcione.
--Generalização :
    Classe generica eh denominada de super-classe ou classe pai e sub-classe eh a filho
--Composicao
    ex: Motor pertence a um carro, um exemplo de tabuleiro de xadrez onde cada classe possui uma casa , possuindo entao 64 casas, (losangulo preenchido)
--Associacao Unidirecional:
    Desejar que essa classe nao conhece seu estado de pertence ou nao pertence ( logasnguo aberto)



" --> " significa dependencia

Softwares para documentação

Modelio.
Umbrello UML Modeller.
Draw.io(online).







------------------------------ Escopo e Encapsulamento -------------------------------------------------


Cap 9.14 - 9.16 (deve ser evitado a quebra de encapsulamento)










---------------Uteis -------------------------------------------------------------

 hour = ( h >= && h < 24 ) ? : 0 ; (if de uma linha so) operador ternario
 minute = ( m>= 0 && m < 60) ? m :0;
 second =  (s >= 0 && s < 60 ) ? s:0;
 
 
 -----------Construtores e Destrutores ---------------------------------------------
 
 
 
*/
