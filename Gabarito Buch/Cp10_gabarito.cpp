/*
 * 10.1
 * a)overloading(sobrecarga) // operator overloading
 * ---------------------------------------------------------------------------
 * b)operator   // operator
 * ---------------------------------------------------------------------------
 * c). :: ?:    //To use operators on class objects, they must be overloaded, with the exception of operators --> '=' ,'&', ',' Can be used in between objects without overloading
 * ---------------------------------------------------------------------------
 d)??         // precedence(can't change the oder of parameters) , associativity(left to right so it continues 4ever) , "arity"(number of operands of a operator cannot be changed)
 * ---------------------------------------------------------------------------
 e)?? // '.' , '?:' , '.*' , '::' 
 * ---------------------------------------------------------------------------
 f)& // delete
 * ---------------------------------------------------------------------------
 g)// new , poiter
 * 
 ---------------------------------------------------------------------------
 10.2 - << mean out of data , like print , exemple of " cout << ", stream out
 *         >> means input of data in a variable or inside of a adress , cin >>
 *  >> -> Extraction of info
 *  << -> insertion of info
 *---------------------------------------------------------------------------
 10.3 - When the programmer need to use the operator like division two
 * ??
 * variables
 *---------------------------------------------------------------------------
 10.4 - True . But i dont know why
 * 
 ---------------------------------------------------------------------------
 10.5 - ??
 * 
 ---------------------------------------------------------------------------
                            OVERLOADING
 The precedence of an operator cannot be changed by overloading. However, parentheses can be used to force the order of evaluation of overloaded operators in an expression.
------------------------------------------------------------------------------
 */
/*
 * 10.6 (Memory Allocation and Deallocation Operators) Compare and contrast dynamic memory
allocation and deallocation operators new, new [], delete and delete [].
 * */

